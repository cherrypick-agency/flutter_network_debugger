name: nd_test_support
description: "Internal test helpers for network_debugger mono-repo (not published)."
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0

